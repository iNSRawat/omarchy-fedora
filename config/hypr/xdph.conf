# xdph.conf — xdg-desktop-portal-hyprland
# https://wiki.hyprland.org/Hypr-Ecosystem/xdg-desktop-portal-hyprland/

[screencopy]
max_fps = 60

# Custom picker binary (optional).
# Leaving this commented uses the built-in native Hyprland screen picker.
# If you install a custom picker (e.g. hyprland-preview-share-picker), uncomment below:
# custom_picker_binary = hyprland-preview-share-picker
